default: 
  defaults: 
    shell: "bash -xe"
  server: 
    scheme: http
    hostname: localhost
    port: 8080
  slaves: 
    - id: slave1
      name: Local Slave 1
      type: local
    - id: slave2
      name: Local Slave 2
      type: local
  campaigns: []
  missions: []
  workspaces: []
