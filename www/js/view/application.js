define("view/application", ["jquery", "view"], function($, View) {

    "use strict";

    return View.extend({

        el: $("body"),

        initialize: function() {
        },

        events: {
        }

    });

});
